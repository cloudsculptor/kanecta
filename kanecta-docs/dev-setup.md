# Kanecta Studio — local dev

    git clone git@github.com:cloudsculptor/kanecta.git
    cd kanecta
    npm install
    npm run dev

Open http://localhost:5173

`npm run dev` will prompt to create a datastore on first run.
