= Architecture
:diagram-format: svg
:icons: font

This document describes the aspirational package architecture for Kanecta.

== Package dependency diagram

[mermaid]
....
flowchart TB
    subgraph apps["Applications"]
        Studio["kanecta-app-studio\n(React SPA)"]
        MCP["kanecta-mcp\n(MCP server)"]
        CLI["kanecta-cli\n(CLI tool)"]
    end

    subgraph studio_internals["Inside kanecta-app-studio"]
        Functions["hosted functions\n(user-provided)"]
        SDK["kanecta-sdk"]
        Functions --> SDK
    end

    subgraph clients["Client layer"]
        ApiClient["kanecta-api-client"]
    end

    subgraph server["Server"]
        API["kanecta-api\n(Express)"]
    end

    subgraph ai["kanecta-ai"]
        ClaudeLib["kanecta-claude-lib"]
    end

    subgraph core["Core"]
        Lib["kanecta-lib"]
    end

    subgraph storage["Storage adapters"]
        FS["kanecta-filesystem"]
        DB["kanecta-database\n+ kanecta-s3"]
    end

    subgraph remote["Remote storage"]
        PG["kanecta-postgres"]
    end

    subgraph external["External services"]
        Claude["Claude API\n(Anthropic)"]
    end

    Studio --> ApiClient
    SDK --> ApiClient
    ApiClient --> API
    API --> Lib
    API --> ClaudeLib
    ClaudeLib --> Claude

    MCP --> Lib
    CLI --> Lib

    Lib --> FS
    Lib --> DB
    DB --> PG
....

== Entry points

[cols="1,3"]
|===
| Entry point | Path

| Studio
| `kanecta-app-studio` → `kanecta-api-client` → `kanecta-api` → `kanecta-lib` + `kanecta-ai/kanecta-claude-lib`

| Hosted functions (inside Studio)
| user code → `kanecta-sdk` → `kanecta-api-client` → `kanecta-api` → …

| MCP server
| `kanecta-mcp` → `kanecta-lib`

| CLI
| `kanecta-cli` → `kanecta-lib`
|===

== Storage adapters

`kanecta-lib` selects a storage adapter at runtime:

[cols="1,3"]
|===
| Adapter | When to use

| `kanecta-filesystem`
| Default. Local JSON files under `~/.kanecta/`. Human-readable, zero config.

| `kanecta-database` + `kanecta-s3` + `kanecta-postgres`
| Remote/hosted mode. Structured SQL storage with S3 for binary assets.
|===
