# kanecta-cli

Command line tool for the Kanecta datastore.

## Usage

```
npm run kanecta
```

That's it. The tool reads the configured item ID from the datastore and generates output in the `kanecta` directory.
