# Grandfathered JavaScript files awaiting migration to TypeScript.
#
# Kanecta is TypeScript-only. NO new .js/.cjs/.mjs source may be added — the
# check in scripts/check-source-languages.sh fails on any tracked JS not listed here.
# (kanecta-app-community-hub is EXCLUDED from the gate and from migration — prod app.)
#
# MIGRATION RATCHET: convert each to .ts and DELETE its line. Goal: zero.
#
# ✅ RATCHET AT ZERO. The JS→TS cut-over migrated every source/test/script file
# across kanecta-core and the tooling to TypeScript + vitest, and the final five
# host-mandated entry points now build from TypeScript via esbuild:
#   * 3 Electron mains  (kanecta-app-studio-{debian,mac,windows}/main.ts)
#   * VS Code extension (kanecta-app-studio-vscode-plugin/extension.ts)
#   * Studio launcher   (kanecta-app-studio/src/server.ts)
# Each of those has a `build` (or `build:server`) script that esbuild-bundles the
# .ts source to the .cjs/.js artifact the host loads directly; the built artifact
# is git-ignored (so it never re-enters this ratchet) and produced on demand by
# the package's start/dist/package scripts. See each package's package.json.
#
# Keep this file (the gate reads it): any NEW entry below is a regression to
# justify in review, not a default. There should be none.
