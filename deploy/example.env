# Copy to deploy/.env and fill in — consumed by deploy/docker-compose.yml.
KEYCLOAK_URL=https://auth.example.com
KEYCLOAK_REALM=my-realm
KEYCLOAK_CLIENT_ID=kanecta-studio
# Recommended: value of the audience mapper you add on the Keycloak client.
KEYCLOAK_AUDIENCE=
STUDIO_PORT=8080
