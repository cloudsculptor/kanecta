# Copy this to .env and fill in values — .env is gitignored, never committed
# Prefer ~/.pgpass for the password (see README.md) — then leave DB_PASSWORD blank

DB_HOST=
DB_PORT=25060
DB_NAME=
DB_USER=
# Only set DB_PASSWORD here if you are not using ~/.pgpass
# DB_PASSWORD=
