{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "referencePayload",
  "type": "object",
  "required": ["targetId", "kind"],
  "additionalProperties": false,
  "properties": {
    "targetId": {
      "type": "string",
      "format": "uuid",
      "description": "UUID of the item being referenced."
    },
    "kind": {
      "type": "string",
      "description": "Nature of the reference. Known values: physical, external, function-arg, function-output, component-dep. Free-form string — new kinds do not require a spec change."
    },
    "medium": {
      "type": "string",
      "description": "For physical kind: engraved, printed, qr-code, label, published-doc. Free-form."
    },
    "location": {
      "type": "string",
      "description": "Human-readable description of where this reference exists (e.g. \"Workshop bay 3, nameplate\")."
    },
    "url": {
      "type": "string",
      "description": "For webhook kind: the endpoint URL. Use $VAR for secrets."
    },
    "blockDeletion": {
      "type": "boolean",
      "description": "When true, deletion of targetId is blocked until this reference item is removed. Default: false."
    },
    "removable": {
      "type": "boolean",
      "description": "Whether this reference can be removed. False for physical engravings that cannot be undone. Informational — does not enforce system behaviour."
    },
    "description": {
      "type": "string",
      "description": "Free-form human explanation of this reference and its implications."
    }
  }
}
